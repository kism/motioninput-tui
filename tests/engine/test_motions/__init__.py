"""Motion tests, one directory per game and one file per character.

A package rather than a plain directory because the same character appears in
several games, so ``test_ryu.py`` is not a unique module name on its own.
"""
